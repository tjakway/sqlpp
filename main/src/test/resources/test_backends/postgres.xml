<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>
    <entry key="TEXT_TYPE">TEXT</entry>
    <entry key="INDEX_TYPE">SERIAL</entry>
    <entry key="INDEX_TYPE_PK_AUTOINC">SERIAL PRIMARY KEY</entry>

    <!-- TODO: may want to separate into keep-in-memory and keep-on-disk blob types -->
    <entry key="BLOB_TYPE">BLOB</entry>

    <comment>A type that can refer to a primary key (e.g. as a foreign key)</comment>
    <entry key="INDEX_REF_TYPE">INTEGER</entry>
    <entry key="DECIMAL_TYPE">DECIMAL</entry>
</properties>
