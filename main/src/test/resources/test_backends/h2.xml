<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>
    <entry key="SQLPP_DEFAULTS_TARGET">SQLPP_DEFAULTS_TARGET</entry>
    <entry key="SQLPP_H2_TARGET">SQLPP_H2_TARGET</entry>
    <entry key="SQLPP_POSTGRES_TARGET">SQLPP_POSTGRES_TARGET</entry>

    <entry key="TARGET_NAME">SQLPP_H2_TARGET</entry>

    <entry key="TEXT_TYPE">VARCHAR(65535)</entry>
    <entry key="INDEX_TYPE">INTEGER</entry>
    <entry key="INDEX_TYPE_PK_AUTOINC">INTEGER PRIMARY KEY AUTO_INCREMENT</entry>

    <!-- TODO: may want to separate into keep-in-memory and keep-on-disk blob types -->
    <entry key="BLOB_TYPE">BINARY</entry>

    <comment>A type that can refer to a primary key (e.g. as a foreign key)</comment>
    <entry key="INDEX_REF_TYPE">INTEGER</entry>
    <entry key="DECIMAL_TYPE">DECIMAL</entry>
</properties>
